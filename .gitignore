node_modules/
dist/
*.log
.DS_Store
.idea/
.env
.env.local
coverage/
*.local.md
*.local.sh
*.local.txt

# SEA build artifacts
dist-sea/
*.blob
cc-candybar-sea-*
demo-recording/
.claude/worktrees/

# Rust client build artifacts (Cargo.lock IS committed — reproducible builds)
rust-client/target/

# Per-platform package binaries — populated only by CI / release.mjs
npm/*/bin/

# Native fast-path binary staged by `just install-rust`. Never committed —
# the committed entry is the node shim at bin/cc-candybar.
bin/cc-candybar-native
.claude/*.lock
