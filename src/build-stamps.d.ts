// [LAW:one-source-of-truth] Substituted textually by tsdown.config.ts's source-digest plugin on every build; absent otherwise, which build-currency.ts reads as a typed "cannot check", never as current.
declare const __SOURCE_DIGEST__: string;
